namespace SendSlackMessage.Enums
{
    public enum EnumResponseType
    {
        in_channel, //Responses are visible to the entire channel
        ephemeral //Responses are just visible to the user
    }
}
